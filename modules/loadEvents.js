/* eslint-disable no-unused-expressions */
import * as myEvents from './eventListeners';

const loadEvents = () => {
  myEvents.openMenu;
  myEvents.closeMenuButton;
  myEvents.closeTheMenu;
  myEvents.closeTheMenuTwo;
  myEvents.closeTheMenuThree;
  myEvents.formError;
};

export default loadEvents;